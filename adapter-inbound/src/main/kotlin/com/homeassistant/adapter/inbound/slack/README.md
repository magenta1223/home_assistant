# Slack adapter — FROZEN

This package is a deprecated legacy access channel. Preserve its current DM, registration, and
`/knowledge` behavior, but do not add commands, modals, scopes, features, or structural refactors.

Allowed changes are limited to fixes for existing behavior, security or data-loss issues, and the
eventual removal of Slack after the HTTP channel replaces it.

